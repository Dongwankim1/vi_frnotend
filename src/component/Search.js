import React from 'react';
import PropTypes from 'prop-types';

const Search = props => {
    return (
        <div>
            search rrr
        </div>
    );
};

Search.propTypes = {
    
};

export default Search;